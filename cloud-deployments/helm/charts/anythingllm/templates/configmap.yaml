apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "AdevHub-AI.labels" . | nindent 4 }}
  name: {{ include "AdevHub-AI.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}