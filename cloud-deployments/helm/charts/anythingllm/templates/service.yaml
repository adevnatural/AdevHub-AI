apiVersion: v1
kind: Service
metadata:
  name: {{ include "AdevHub-AI.fullname" . }}
  labels:
    {{- include "AdevHub-AI.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AdevHub-AI.selectorLabels" . | nindent 4 }}
